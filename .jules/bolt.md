## 2024-05-23 - [Frontend Memoization Pattern]
**Learning:** Extracting components to `src/components` and using `React.memo` combined with `useCallback` for event handlers effectively isolates re-renders.
**Action:** Always check if high-frequency state updates (like typing) trigger unnecessary re-renders of unrelated UI parts.
